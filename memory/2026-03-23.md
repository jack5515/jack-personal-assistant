# 2026-03-23

## 今日关键进展

- 排查并重建了 `stock-finance-daily-briefing` 每日任务。
- 已确认股票财经晨报任务在当前 Mac 上真实落地，工作日每天 `09:00` 推送到 Jack 飞书私聊。
- 当前股票晨报为最小可用版，先用 OpenClaw agent + 公开信息能力运行；`tushare / baostock / pandas` 与相关 token 在当前机器上均未配置，后续可升级为用户偏好的国内数据源版本。

- 排查了 `lenny-daily-ai-product-cards` 未送达问题。
- 已确认原问题不在内容生成本身，而在 `announce` 投递链路不稳定；一次真实报错为多模型超时。
- 已将 Lenny 卡片任务改轻：固定输出 `5` 条、先写本地结果文件 `workfiles/lenny-daily-cards-latest.md`。
- 已新增独立发送脚本 `scripts/send-lenny-daily-cards.sh`，并挂上系统 `crontab`：每天 `10:02` 直发到 Jack 飞书私聊。
- 发送脚本已加入“只发今天内容 + 每天只发一次”保护，状态文件为 `memory/lenny-daily-cards-send-state.txt`。
- 已实测：Lenny 卡片补发成功，Jack 确认收到。

## 用户确认的新记忆

- Jack 要求把当前群聊回复策略写入长期记忆。
- 已更新 `MEMORY.md`：默认群聊策略为“少说、只在有增量价值时说、优先像正常群成员、不当 Jack 的代言人、默认潜水但必要时高价值发言”。
- Jack 新增要求：后续任务默认按 `主 agent + 子 agent` 形式组织；主 agent 负责统筹和最终交付，子 agent 负责专项执行、专项判断或交叉校验。

## 当前待办/后续可做

- 可把股票晨报也升级为“两段式稳态链路”（生成与发送解耦），进一步降低偶发投递风险。
- 可在后续补齐 `tushare + baostock` 环境后，把股票晨报切到用户偏好的国内数据源版本。
