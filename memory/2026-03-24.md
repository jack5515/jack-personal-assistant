# 2026-03-24

- Jack 明确确认后续 PRD 默认写法：开头只保留 `目标 / 核心原则 / 页面跳转总流程 / 通用补充` 四块；主体按 `场景 x 页面` 展开，页面维度固定为 `首页入口 / 输入页 / 执行中状态页 / 结果页`。
- PRD 内容要求：聚焦功能逻辑、页面文案、按钮文案、跳转关系、结果模块；不写用户背景、用户问题、产品判断等铺垫。
- PRD 格式要求：优先表格；表格内用有序列表和无序列表组织内容，不要在单元格里堆大段文字；目标是让开发一眼读懂并可直接据此推进实现。
- 后续如果 Jack 说“按那个 PRD 模板来”，默认按上述结构输出。
- 今天新增一组重要创业判断：针对“面向 3-30 人小团队的 AI Layer”方向，当前最优收敛定义是 `Slack-first 的团队决策与跟进层`，核心不是再做一个聊天助手，而是做 `决策到执行的工作操作层`。
- 该方向的关键链路被收敛为：`capture Slack thread -> confirm decision / owner / due date / action items -> sync 到 Notion / Linear / GitHub -> 自动 follow-up`。
- 对该 AI Layer 创业方向的核心判断：方向本身成立，ICP、MVP、workspace 定价和 design partner 模式都较合理；最大待验证点不是产品能否做出来，而是 `值得 capture 的线程频率是否足够高` 以及 `用户是否愿意形成 confirm 习惯`。
- 当前更推荐把产品定义为 `执行收口层` 而不是 `摘要层`；真正值得押注的差异化不是 summary，而是 `nudge / no-owner / overdue / follow-up loop`。
- 对连接器优先级的新判断：MVP 第一阶段不应同时强调 Notion / Linear / GitHub，应该先围绕一个主写入目标打透，降低 onboarding 和价值感知分散。
- 今天还收敛了一组 Agent 产品分层判断：不要只用 `CLI vs App` 来看 Agent 产品，更有解释力的分层是 `能力底座 / 专家入口 / 结果代理 / 信息入口`。
- 对典型产品的当前默认理解：`OpenClaw` 更像 Agent runtime / 底座；`Codex CLI` 更像高手/专家入口；`Manus` 更像结果代理 / 任务服务；`Genspark` 更像被 Agent 重写过的信息入口。
- 这组判断的核心不是“谁更先进”，而是它们分别在卖什么：`能力密度 / 底座能力 / 结果交付 / 信息入口`。
- Jack 新增明确要求：后续 `AI 巡检简报` 默认要结合创业来做，不只汇总信息；若某条 signal 对创业方向、切入点、产品定义、收费、护城河、时机判断有启发，需要主动提炼 `创业启发`，并把高价值 insight 沉淀进创业背景知识。
- 已新增一份 `memory/2026-03-24-startup-insights-from-briefings.md`，把近期 AI 巡检任务、Lenny AI 产品学习卡片及相关判断重新按“创业启发”做了一次回溯整理，重点沉淀：方向原型优先、轻资源验证、Agent 先高控制低自治、多 agent 管理层、上下文外包价值、以及“高价值 thesis engine”式创业机会。
