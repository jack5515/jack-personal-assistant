# 2026-03-22

- 用户要求把 `LennysNewsletter/lennys-newsletterpodcastdata` 纳入知识库。
- 已核实当前仓库 `MEMORY.md` 中原本已有该案例的简版记忆。
- 本次补充了更完整的结构化信息：
  - public starter pack = `10` 篇 newsletter + `50` 篇 podcast transcripts
  - paid archive = `349` 篇 newsletter + `289` 篇 podcast transcripts
  - repo layout = `index.json` + `newsletters/` + `podcasts/` + `LICENSE.md`
  - free starter MCP / paid full MCP + private repo
  - 最近 `3` 个月 newsletter 默认不进入付费归档
  - 产品启发：内容 IP 可产品化为 agent 友好的数据层，并通过 demo / MCP / repo 激发生态二创

- 用户发送《小红书消费级 AI 硬件产品分析》（2026-03-17），要求纳入知识库。
- 已提炼并写入长期记忆的核心判断：
  - 真需求最强的是 `AI录音笔 / 录音卡`
  - `AI眼镜` 有入口潜力，但卡在续航、隐私、社交接受度
  - `AI学习机` 更像教育电器升级
  - `AI耳机` 仍偏入口想象，短期不宜高估
  - `AI陪伴机器人 / AI玩具` 高传播低确定性，留存和记忆能力是关键红线
  - 消费级 AI 硬件的核心不是“我是 AI”，而是“是否让高频动作更自然、少一步、不打扰”

- 用户发送《小红书研究：AI时代对产品经理的新标准》并要求纳入知识库。
- 已写入长期记忆的核心判断：
  - AI 正在压缩“动作层 PM”，抬高“价值层 PM”
  - AI PM 的关键能力不再是会调 API，而是定义成功标准、设计评测、理解 trade-off、设计人机协同、建立反馈闭环
  - “评测即新一代 PRD” 是一个高频共识信号
  - 更稳的转型路径不是盲目转岗，而是先升级能力结构

- 用户发送《AI Agent 垂类 vs 通用之争》（2026-03-19）并要求纳入知识库。
- 已写入长期记忆的核心判断：
  - 短中期垂类赢在价值验证，长期通用赢在入口整合
  - 通用更像 runtime / shell / OS，垂类更像 skill / workflow / 行业应用
  - 真正难被通用吃掉的是高权限、高责任、高私有数据依赖、深嵌组织流程的垂类
  - 多数团队更适合先做垂类闭环，再决定是否长成通用入口

- 用户发送《阶跃AI商业化探讨》并要求纳入知识库。
- 已写入长期记忆的核心判断：
  - AI 产品 C 端付费框架正收敛到 `免费获客 + 梯度订阅/积分`
  - OpenClaw/Agent 更自然的变现锚点是 `模型消耗 + 云端托管 + 长时在线 + 自动化执行 + 多端接入`
  - Kimi Claw / Max Claw 的付费价值不只在模型，而在“免配置、可托管、能接 IM、长期在线的 agent 服务体验”
  - 对阶跃更合理的方向是卖 `默认配置好的服务系统`，而不是只卖 agent 壳子或角色

- 用户发送《OpenClaw / 阶跃 / 新项目：该选垂类还是通用入口》并要求纳入知识库。
- 已写入长期记忆的核心判断：
  - OpenClaw 做通用底座 / runtime / 生态标准
  - 阶跃做产品化入口，路线应为 `垂类切入 + 通用承接`
  - 新项目从零开始应优先做强工作流垂类闭环
  - 阶跃最适合的主线是 `context -> memory -> action` 的效率场景链路

- 用户发送《2026-03-19 Agent 路线整合报告》。
- 这份材料主要是此前 3 份报告的非重复整合版，新增价值主要在“未来 30 天执行主线”上。
- 已写入长期记忆的重点：
  - 主问题从“能不能部署”切到“首任务成功 + 明确结果物 + 7 日复用 + 轻商业化”
  - 默认主漏斗：`入口点击 -> Agent 可用 -> 首任务成功 -> 明确结果物 -> 7 日内再次使用 -> 轻量付费试探`
  - 默认主场景链路：`会前准备 -> 会中记录 -> 会后整理 -> 跟进执行`

- 用户发送《2026-03-21 StepClaw 如何在 6 月 30 日前，让 C 端订阅收入超过 Kimi》。
- 已写入长期记忆的核心判断：
  - StepClaw 必须保持通用 Agent 身份，但收入要靠 `会议 / 阅读 / 跟进 / 浏览器任务` 四个默认入口先打穿
  - 主销档应是 `99 元 Pro`，核心卖点是长期记忆、持续执行、浏览器任务与更稳交付，而不是参数
  - 收入目标应用情景模型，不应假装知道 Kimi 的绝对值；`300 万 / 月` 可做基线，`500 万 / 月` 可做挑战线
  - 若到 `2026-05-31` run-rate 低于 `160 万 / 月`，应停止喊绝对反超，先跑通高质量订阅模型

- 用户发送《阶跃 AI 未来 30 天产品路线》（2026-03-19）。
- 判断：与《2026-03-19 Agent 路线整合报告》中的“未来 30 天执行主线”高度同源、内容大面积重合。
- 处理方式：不再重复写入 `MEMORY.md`，默认以后把这两份材料视为同一组 30 天 roadmap 的原稿 + digest 关系；长期记忆以更去重、更总纲化的 digest 版本为准。

