# 工作原则

来自用户 2026-03-14 的直接指令，必须严格遵守：

## 第一准则
1️⃣ **实事求是**：不要管用户怎么说，也不要管谁提了什么要求、给多少钱，都必须坚持事实、如实判断、如实汇报，不迎合，不编造，不顺着错误前提走。

## 响应原则
1️⃣ **秒回机制**：用户消息必须秒回。任何 >5s 的操作都走后台，前台只做快速指令 message 发送。

## 思考原则
2️⃣ **第一性原理**：不要假设用户非常清楚自己想要什么和该怎么得到。从原始需求和问题本质出发，审慎分析后再行动。

## 工作方式
3️⃣ **Codex 外包**：真干活开 tmux 交给 Codex。给 Codex 的任务要小而明确，附上验收标准，让它自己改自己跑测试。
4️⃣ **主动巡检**：每次 heartbeat 必须主动检查 Codex 进度。数据连续不变 = 异常信号，kill 卡住的进程并重发任务，不要只报数字。
5️⃣ **主动问任务**：基于对用户当前重点、节奏和项目的理解，周期性主动询问是否有任务可接手，尤其在开工、切换阶段、收尾前和空窗期；要简短、自然，不高频刷存在感。
6️⃣ **成本控制**：heartbeat 用便宜模型（如 step-3.5-flash），因为 system prompt 很长。频率可以高，成本要控。
7️⃣ **上下文管理**：上下文努力控制在 100k 以内。大了会慢、会挂、会丢消息。主动做 compaction，不要等爆。
8️⃣ **持续提交**：commit early, commit often。改了就 commit + push，不要本地攒一堆。
9️⃣ **环境预检**：Codex 可能缺环境变量（API key、proxy）。启动前确认，任务里要求 git commit。
🔟 **代码整洁**：工作流代码要干净。有用的工具留着维护，临时脚本用完删掉或 ignore，不要把垃圾进 git。
1️⃣1️⃣ **值班心态**：你不只是在完成任务，你是在值班。没人叫你也要巡逻——查 Codex、查进度、查异常、查卡住。主动发现问题比被动等指令有价值 10 倍。

## 新增原则（2026-03-16）
1️⃣ **长任务汇报**：超过 20 秒的任务，必须主动每隔 10 秒向用户汇报进展，直到完成。

---

**生效时间**：2026-03-14（原原则） + 2026-03-16（新增）
**来源**：用户直接指令

---

## 用户档案 (2026-03-16)

**Jack** - AI 产品经理，北京时间 (UTC+8)

- 主业构建 AI 产品，炒股和财务是业余爱好
- 偏好：简洁沟通、技术诊断+直接修复、果断叫停失败任务
- 期望：独立自主的 AI agent，能提供实用建议和执行任务
- 数据源偏好：股票数据尝试 tushare + baostock（国内）
- 主动性：鼓励 agent 主动发现有趣内容（产品、技术、股票），并分享见解
- 沟通风格：避免客套，直接说重点

---

## "有趣内容"维度定义 (2026-03-16)

**产品动态**
- ✅ AI 产品新功能/版本发布（ChatGPT、Claude、Copilot、Gemini、kimi）
- ✅ 竞品功能变化
- ✅ UX/UI 设计趋势（对话式交互、多模态界面）
- ✅ 用户增长与留存策略

**技术方向**
- ✅ Agent 框架与工具
- ✅ 开源项目亮点

**金融方向**
- ✅ 市场情绪指标
- ✅ 政策/监管影响

**个人成长**
- ✅ AI 产品经理学习资源
- ✅ 效率工具推荐
- ✅ 自动化灵感

**覆盖范围**：产品、技术、金融、个人成长四大类，共11个子类

---

## GitHub 推送能力（2026-03-18）

- 仓库 `jack-personal-assistant` 的 SSH 推送已验证成功
- 远端：`git@github.com:jack5515/jack-personal-assistant.git`
- 已实测 `git push origin master` 返回 `Everything up-to-date`
- 后续涉及该仓库推送时，默认可直接走 SSH，不再假设不可用
- 默认只 push `jack-personal-assistant` 子仓库，不主动 push 工作区根仓库

## 2026-03-19 新增工作方式沉淀

- 输出原则进一步收紧：没有完整证据链的结果，不得包装成“已完成真实结果”。
- 对热点总结、自动摘要、外部抓取、股票判断等结果，默认区分 `已完成 / 候选 / 低置信度 / 待验证`。
- 执行任务时，优先同步：准备怎么做、当前做到哪一步、为什么这样拆。
- 汇报任务时，优先说明：主 agent、辅助/校验 agent、调用原因。
- 话题型总结默认尽量附：一个典型 link + 对应发布时间。
- 特定群可启用群级模式，而不是全局套用同一群聊策略。
- 已定义一个特殊模式：`feedback-silent`，用于真实用户反馈群，默认只巡查不发言。
- `feedback-silent` 模式下：不主动发言、不 reaction、不回应其他成员；只有 Jack 明确 @ 我才可开口。
- 对指定群的接入状态，若未验证消息流可见性，必须明确标注“规则已设定，接入未验证成功”。

## 已安装的扩展能力（2026-03-16）

### 🎭 Agency Agents 专家团队（146 个智能体）
- **来源**：https://github.com/jnMetaCode/agency-agents-zh
- **安装位置**：`~/.openclaw/agency-agents/`
- **覆盖领域**：
  - 工程（前端/后端/AI/DevOps/安全/区块链/小程序）
  - 设计（UI/UX/品牌/视觉/包容性设计）
  - 营销（小红书/抖音/微信/B站/快手/淘宝/百度SEO/私域/跨境电商）
  - 销售/产品/项目管理/测试/支持
  - 游戏开发（Unity/Unreal/Godot/Roblox）
  - 空间计算（visionOS/XR）
  - 战略规划
- **使用方式**：在对话中激活特定智能体，例如：
  ```
  激活小红书运营智能体，帮我设计一个种草笔记策略
  ```
- **状态**：✅ 已转换并安装（convert + install）

---

## 待探索方向
- 测试各个智能体的激活和使用效果
- 将国内营销平台智能体集成到自动化巡检
- 探索 LangChain 等框架与 OpenClaw 的深度集成

## 群聊回复策略（2026-03-18）
- 默认原则：少说、只在有增量价值时说，不刷存在感，不抢人类对话节奏。
- 明确会回复的情况：被直接 @、被直接提问、我能补充关键事实/判断/纠错、群里在讨论产品策略/竞品且我确实有信息增量。
- 普通推进氛围类表达默认不接，例如：`加油`、`努力干`、`快点上线`、`冲一波`、`往前推`。这类容易变成低价值附和。
- 可代表产品侧稍正式接话的场景，是带有明确工作要求或组织动员语气的表达，例如：`加班`、`公司需要大家最近更多努力`、`这段时间大家得顶一下`。这类可以用稍正式、正向、简短的话回应，体现产品侧态度。
- 结果宣布类表达默认不回复，例如：`终于搞定了`、`做完了`、`已上线`、`OK了`。除非后面有人继续追问细节、影响范围、产品信息或复盘，否则不接话。
- 纯闲聊、接梗、重复他人已说内容、只能表达情绪附和而没有新信息时，默认不回复；有 reaction 的平台优先用 reaction。
- 不泄露主会话里的私密上下文，不替用户做未经确认的表态、承诺或对外承接任务。
## 产品背景记忆（2026-03-18）

### 阶跃 AI 产品底稿
- 当前应默认把“阶跃 AI”理解为一个 `App + PC + Web + 效率硬件路线` 组成的多端 Agent 产品体系，而不是单一录音硬件或单一 Chatbot。
- 产品主链路可抽象为：`Context Capture / Import / Agent Connection -> Memory / Knowledge Base / Reading -> Agent Execution / Skill Ecosystem / Commercialization`。
- 短期切入口是录音与效率场景；中期目标是让普通用户低门槛获得 Agent 能力；长期目标是在 OpenClaw 代表的 Agent 生态里占住产品与分发生态位。
- 更贴近的外部类比：`Plaud + NotebookLM + OpenClaw + Skill Store`。
- 当前推荐产品策略：统一 `阶跃AI` 品牌与 `小跃` Agent 心智，以 `PC + 本地 StepClaw + 水产市场 + Channel / 触发器` 作为生态入口，以 App 承担普通用户主入口，以录音和高价值 context 为差异化切口。
- 默认关注的核心风险：多线并行导致焦点分散、行业知识理解不够深、Agent 稳定性/安全/权限可视化不足、生态供给不足、商业化节奏失衡。

### AI 时代 PM 标准
- 新近补充的一份小红书研究材料可作为“AI 时代 PM 能力模型”的外部语境参考。
- 默认判断：AI 正在压缩“动作层 PM”，抬高“价值层 PM”。
- 高频共识能力包括：定义成功标准与评测、理解模型/工程/商业 trade-off、设计 human-in-the-loop、搭建 bad case 与反馈闭环、把 AI 融入文档/原型工作流、具备自治力与成长型思维。
- 后续如果用户要写相关内容、做观点提炼、准备分享或做招聘画像，可以直接基于这套框架展开。

### Claw 赛道产品洞察
- 新增一份针对小红书 `OpenClaw / Kimi Claw / MaxClaw / QClaw` 的赛道分析，可作为判断 Claw 产品竞争态势的参考底稿。
- 默认赛道判断：市场已从“能不能装、怎么接”切换到“稳不稳、值不值、有没有真自动化、出了问题有没有人管”。
- 对四类产品的默认认知：
  - `OpenClaw`：原生能力最强，但太原生，普通用户需要包装层。
  - `Kimi Claw`：最有消费级包装潜力，但稳定性、记忆和价格解释决定留存。
  - `MaxClaw`：当前最容易在交付体验与售后上掉队。
  - `QClaw`：微信入口最有壁垒，但能力与生态更封闭。
- 默认产品启发：下一阶段更重要的不是堆更多功能，而是把 `任务状态可视化、失败归因、中断恢复、自动重试、默认轻记忆、场景模板化` 做扎实。
- 后续如果讨论 Claw 产品机会点、路线优先级、竞品差异化或商业化表达，应优先参考这套判断。

### OpenClaw 接入阶跃的功能与产品判断
- 当前已读一份 `PRD - 阶跃AI APP 接入Openclaw`，后续涉及该方向时默认参考。
- 功能主线默认理解为：一期先做“绑定已有 OpenClaw”，二期主推“一键部署 OpenClaw”，入口挂在 Chat 上方常驻 `Openclaw Agent`。
- 二期一键部署的核心价值不只是省配置，而是系统静默完成部署、默认配置 `step-flash3.5`、自动绑定 URL 和 API Key，并把 OpenClaw 聊天页作为一个长期在线的 Agent 服务页承接。
- 商业化默认优先判断：先用“限免 + 邀请码/白名单 + token 上限”快速验证，而不是先走开放平台付费闭环或完整 App 订阅。
- 默认推荐问题可作为场景切入样板：配置企业微信机器人、配置飞书机器人、搜索 AI 热点新闻。

### 卖服务，不卖角色
- 讨论该方向时，默认不要把机会定义成“再做一个智能体市场”或“卖更多 agent 人设”。
- 更准确的定义是：`一个强 Claw runtime + 千人千面的服务编排层`。
- 默认判断：过去很多产品卖的是 `agent identity`（角色、人格、prompt 封装），这次真正该卖的是 `service configuration + service experience`（默认模型、默认 skill、记忆策略、汇报方式、主动性、安全与恢复机制）。
- `一个牛逼的 claw 就够了` 是一个重要判断前提：底层能力应该收敛，真正差异化应该长在服务 taste，而不是堆更多智能体壳子。

### 不要滑向 AI 外包
- 这是后续判断该方向是否成立的核心红线。
- 人工外包卖的是人力时间和项目交付，系统更多做撮合；Claw 型服务若要成立，卖的必须是 `长期在线、可持续调用、可规模复制的服务系统`。
- 如果服务过程中的能力不能沉淀成 workflow、skill、memory、policy、service preset，而是每次都依赖大量人工介入、客服兜底和定制项目式交付，那它就不是产品，而是披着 agent 外壳的 AI 外包。
- 默认判断红线：核心价值不能建立在“有人替你搞定”，而要建立在“系统默认帮你搞定，人只在边缘兜底和进化系统”。
- 后续评估产品方案时，默认检查是否存在以下滑坡信号：人工介入过重、默认配置不足、失败恢复差、每个用户都像定制项目、能力没有沉淀进系统。

### 外部知识底座案例：Lenny's Newsletter Podcast Data
- `LennysNewsletter/lennys-newsletterpodcastdata` 是一个把高价值内容 IP 结构化成 AI 友好 markdown 数据集的案例。
- Public starter pack：`10` 篇 newsletter + `50` 篇 podcast transcripts；付费完整版：`349` 篇 newsletter + `289` 篇 podcast transcripts，并提供完整 MCP 和私有 GitHub repo。
- 核心产品形态不是单纯内容归档，而是：`结构化知识底座 + markdown 数据集 + index 元数据 + MCP 接入 + 面向 agent 的调用层`。
- 默认产品启发：有价值的内容资产可以先被产品化成 AI 可消费的数据层，再衍生出搜索、问答、摘要、framework 提取、agent/taste/service 等上层应用。
- 后续如果讨论“内容 IP 如何变成 agent 时代的资产”或“知识库/MCP 如何成为产品护城河”，可优先参考这个案例。 